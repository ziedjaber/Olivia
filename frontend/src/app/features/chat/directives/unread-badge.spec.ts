import { UnreadBadge } from './unread-badge';

describe('UnreadBadge', () => {
  it('should create an instance', () => {
    const directive = new UnreadBadge();
    expect(directive).toBeTruthy();
  });
});
